# swxt 商城后台管理系统 — 开发模块列表

> 依据《培训任务2.docx》整理，结合项目当前代码现状（后端 `src/main/java`、前端 `frontend/src`、`swxt.sql`）标注实现状态。
> 最近更新：2026-08-06（模块 4 商品详情、模块 5 综合查询已完成并实测：新增 GET /products/{id} 详情接口（含分类名）、商品列表综合筛选 keyword/categoryId/priceMin/priceMax/status、GET /users 支持 keyword；前端商品管理加筛选栏、商城详情接入独立接口）

## 一、模块总览（含现状）

| # | 模块 | 文档要求 | 状态 |
|---|------|---------|------|
| 1 | 登录 / 注册 | 用户进入系统 | ✅ 已实现并验证（JWT + Security；三角色：admin 管理员、operator 操作员、zhangsan 等普通用户，密码均 123456） |
| 2 | 商品管理 CRUD | 商品增删改查（分类、图片、名称） | ✅ 已实现并验证（分页/搜索/新增/修改/删除闭环测试通过；图片支持本地上传，POST /upload 仅管理员，SecurityConfig 放行 GET /uploads/**） |
| 3 | 商品分类管理 | 分类增删改查（电器、玩具、零食…） | ✅ 已实现并验证（2026-08-05 上线：CategoryController + CategoryService + CategoryMapper 补齐 insert/update/deleteById；前端 ProductManager 内嵌 CategoryManager 面板，分类 CRUD 实测全通过） |
| 4 | 商品详情 | 查看特定商品详细信息 | ✅ 已实现并验证（2026-08-06：后端新增 GET /products/{id}（联表 category 返回分类名称）；管理端 ProductManager「查询」与用户端 Shop「查看详情」均接入独立接口，详情面板展示分类名；无效 id 返回 404） |
| 5 | 综合查询 | 条件/精确/模糊查询（分类、价格、名称） | ✅ 已实现并验证（2026-08-06：GET /products 支持 keyword 名称模糊、categoryId 精确、priceMin/priceMax 区间、status 组合筛选；GET /users 支持 keyword 模糊；前端 ProductManager 加分类/价格/状态筛选栏，商品列表与详情均展示分类名） |
| 6 | 系统配置管理 | 背景图、每页条数、是否开启日志 | ✅ 已实现并验证（14 项配置，读写均正常） |
| 7 | 系统日志管理 | 查询 / 清空日志 | ✅ 已实现并验证（GET /system-logs 分页+筛选、DELETE /system-logs 清空；前端 SystemLogs.vue 已接真实接口并有清空按钮；仅时间筛选未做） |
| 8 | 敏感操作日志记录 | 登录及增删改操作写日志（时间+用户+操作+IP） | ✅ 已实现并验证（登录成功/失败、注册成功/失败、登出、用户删除/更新、商品增删改、分类增删改、库存出入、订单创建/改状态、配置修改、日志清空均已自动记录；注册失败日志 2026-08-05 修复接入） |
| 9 | 用户管理 | 用户增删改查 + 权限设定 | ✅ 已实现并验证（分页/搜索/更新/删除/角色权限，含 OPERATOR 角色） |
| 10 | 权限菜单区分 | 不同权限显示不同菜单 | ✅ 已实现并验证（后端 @PreAuthorize 实测生效；前端路由守卫按三角色区分 /admin/*、/operator/*、/user/*；2026-08-05 修复：越权访问现返回标准 code=403，不再返回 500） |
| 11 | 微信扫码登录 / 绑定 | 微信登录与绑定 | ❌ 未实现 |
| 12 | 业务测试 | 系统完成后验收 | 🟡 进行中（2026-08-03 首轮、2026-08-05 二轮验证，见文末） |

> 注：代码中另有「订单」与「出入库」两个功能模块（培训任务外的扩展实现），均已实测通过，见「三、已实现内容」与「五、功能验证记录」。

## 二、待开发模块详细拆分

### 模块 A：商品详情（对应总览 #4）— ✅ 已完成（2026-08-06，实测通过）
- 后端：GET /products/{id} 已实现（与 DELETE /products/{id} 共存），联表 category 返回分类名称（Product 实体新增 categoryName 字段，列表/搜索/详情查询统一联表填充）；无效 id 返回 404
- 前端：Shop.vue「查看详情」改调独立接口 GET /products/{id} 拉取最新数据（失败回退列表行数据），详情面板展示分类名称；管理端 ProductManager「查询」详情面板沿用编辑表单（分类仍以 ID 输入以便编辑）

### 模块 B：综合查询 — ✅ 已完成（2026-08-06，实测通过）
- 后端：GET /products 支持 keyword（名称模糊）、categoryId（精确）、priceMin/priceMax（价格区间）、status，任意组合动态拼接；GET /users 支持 keyword（用户名/真实姓名/手机号/邮箱模糊）；全部条件为空时回退全量分页；旧接口 /products/search、/users/search 保留兼容
- 前端：ProductManager 新增筛选栏（分类下拉 /categories、价格区间、状态）＋名称搜索框，重置一键清空；商品列表分类列显示分类名

### 模块 C：系统日志（剩余项）
- 前端时间筛选未实现（注册失败写入已于 2026-08-05 修复，见问题清单 #5）

### 模块 D：微信扫码登录与绑定（依赖外部资源）
- 后端：测试公众号 appId/appSecret 配置；GET /wechat/qrcode 生成登录二维码；授权回调用 code 换 openid → 已绑定直接登录，未绑定引导绑定；POST /wechat/bind 绑定 openid（User.wechatOpenid 字段已预留）
- 前端：登录页增加"微信扫码登录"入口与绑定页
- 外部依赖：需申请微信公众平台测试公众号，且服务须部署到可被微信回调的云服务器（文档明确要求）

### 模块 E：业务测试验收
- 按文档逐条验收，核对日志格式样例：
  xxxx.xx.xx xx:xx:xx  admin  登录系统  192.168.2.1
- 已完成的 2026-08-03 首轮、2026-08-05 二轮验证见文末，可在其基础上逐项勾选

## 三、已实现内容（复核，避免重复开发）
- 角色体系：ADMIN（系统管理）/ OPERATOR（操作员：库存出入、订单处理）/ USER（普通用户：商城购物）
- 后端：UserController（登录/登出/注册/用户 CRUD/商品 CRUD/用户与商品搜索/商品详情 GET /products/{id}/商品综合筛选 keyword+categoryId+priceMin/priceMax+status/用户 keyword 搜索）、CategoryController（分类 CRUD，登录可查列表、增删改仅 ADMIN）、OrderController（下单校验库存不扣减/我的订单/全部订单分页+状态与关键词筛选/订单详情/改状态，下单成功后自动清空购物车）、StockController（入库/出库/记录查询/按订单出库 POST /stock/order/{id}/out/按订单退货入库 POST /stock/order/{id}/in，OPERATOR）、CartController（购物车：GET 列表 / GET count / POST 加入累加 / PUT 改量 / DELETE 删项 / DELETE 清空，登录用户数据按 user_id 隔离）、FileController（POST /upload 图片上传，仅管理员）、FileService（UUID 重命名保存 ./uploads/product/、扩展名白名单 jpg/jpeg/png/gif/webp、deleteByUrl 删除本地文件，商品删除时同步清理）、SystemConfigController（GET/PUT）、StatisticsController（统计面板/库存流水，流水查询已开放 OPERATOR）、SystemLogController（GET 分页+筛选 / DELETE 清空）、SystemLogService（日志写入 + 客户端 IP 提取）、JWT 安全体系（SecurityConfig 放行 GET /uploads/**）、全局异常处理、CORS
- 实体：User（含 wechatOpenid）、Product、Category、SystemConfig、SystemLog、OrderInfo、OrderItem、StockRecord、CartItem
- Mapper：UserMapping、ProductMapping 相关方法（含分页/搜索/updateStock 防负库存）、CategoryMapper（listAll/findById/insert/update/deleteById）、SystemConfigMapper、StatisticsMapper、SystemLogMapper（insert + searchLogs + deleteAll 清空）、OrderMapper（insertOrder/insertOrderItem/查询/改状态/分页筛选 listAllPage+countAll）、StockRecordMapper、CartMapper（列表 join 商品 / count / 加入累加 / 改量 / 删项 / 清空）
- 前端：Login / Register / UserHome / Shop（商城列表+加购，调 /cart 接口）/ Cart（购物车，数据库存储：/cart 接口增删改查，结算调 POST /orders 下单后后端自动清空）/ AdminHome / UserManagement（含搜索，新建用户角色含 OPERATOR）/ ProductManager（含搜索、创建/编辑表单图片上传、图片地址规范化、内嵌分类管理面板 CategoryManager）/ Statistics / SystemSettings / SystemLogs（已接真实接口，含清空）/ OperatorHome / OrderManagement（操作员订单管理：分页+状态/关键词筛选+详情，路由 /operator/orders）/ StockManager（商品出入库与订单出入库双标签，订单 tab 可直接出库/退货入库）/ StockLog / OperatorSettings；公共组件 Pagination（页码序列为当前页前后连续）/ SearchToolbar / SettingsPanel / StockRecordTable；utils/request.ts 封装 fetch(get/post/put/del/upload)；路由守卫按三角色区分
- 数据库：user、product、category、system_config、system_log、stock_record、orders、order_item、cart 九张表（cart：user_id+product_id 唯一，重复加入累加数量；下单成功后清空该用户购物车）

## 四、建议开发顺序
C（系统日志·剩余：前端时间筛选）→ D（微信扫码）→ E（业务测试）
> A（商品详情）、B（综合查询）已于 2026-08-06 完成并实测，不在待开发列表。
> 分类管理、订单、库存已于 2026-08-05 完成，不在待开发列表。

## 五、功能验证记录

### 2026-08-03 首轮验证
> 验证环境：JDK 17 + Maven 3.9.16、MySQL 8.0.46（swxt 库）、Node 26 + Vite 8。后端 8887 端口、前端 dev server 5173 端口。

#### 验证通过项
- 后端 `mvn package`：BUILD SUCCESS，`SwxtApplicationTests` 通过
- 登录（admin/123456）返回 JWT + ADMIN 角色；公开接口 /system-config/public 正常
- 用户分页/搜索、商品分页/搜索、统计面板、库存流水、系统配置读写：全部 code=200
- 安全拦截：无 token / 错误 token → 403；USER 角色访问 ADMIN 接口被拒绝
- 注册开关：allow_register=false 时注册被拒（code 1001）
- 商品增删闭环：创建 → 搜索 → 删除成功，无数据残留
- 前端 `npm run build` 成功；dev server 页面、组件模块、/api 代理均正常

#### 本次发现并修复的问题
1. **前端语法错误（已修复）**：`frontend/src/components/ProductManager.vue:230` 正则 `/^\\//` 非法（TS1109），导致 dev server 无法启动、build 失败；已改为 `/^\//`（匹配以 `/` 开头的图片路径），修复后构建与运行正常
2. **system_config 中文乱码（已修复）**：11 行 config_value 与 description 均为双重编码损坏（历史遗留，验证前已存在）；已还原为正确中文（如 site_name=商城管理系统），修复前建了备份表 `system_config_bak`（可回滚）
3. **残留测试数据（已删除）**：product 表 id=29 的乱码测试商品已删除，商品总数恢复 25

### 2026-08-05 二轮验证（本次）
> 环境：JDK 17 + Maven 3.9.16（PATH 默认 Java 8 需显式 `set JAVA_HOME=D:\Program Files\Java\jdk-17`）、MySQL 8.0.46、后端 8887 端口运行中。命令：`mvn test` 与接口实测（PowerShell Invoke-RestMethod，脚本见 `.preview/verify-backend.ps1`）。

#### 验证通过项
- 后端 `mvn test`：BUILD SUCCESS，`SwxtApplicationTests` 通过（JDK 17 下）
- 登录：admin / operator / zhangsan 三个角色均返回 JWT
- 分类管理 CRUD：GET /categories（25 条）→ POST 新增 → PUT 改名 → DELETE 删除，全通过
- 商品分页 GET /products：success=true，total=100（数据较首轮有新增）
- 库存：POST /stock/in（+5）、POST /stock/out（-2）、GET /stock/records、GET /stock/records/1 全通过（OPERATOR）
- 订单闭环：POST /orders 下单（PENDING、扣库存 123→122）→ GET /orders/my → GET /orders（ADMIN 全量）→ GET /orders/{id} 详情（含 items）→ PUT /orders/{id}/status?status=SHIPPED，全通过
- 权限：USER 访问 GET /orders（OPERATOR/ADMIN 接口）被拦截（HTTP 200 + code 500 "Access Denied"，拦截生效但语义不标准）
- 系统日志 GET /system-logs：total=156；系统配置 14 项读写正常；统计面板正常
- 前端 `npm run build`：成功（vue-tsc + vite build 通过）
- 验证残留数据已清理回滚：订单、2 条 stock_record 已删，商品 1 库存恢复 120

#### 遗留说明（承接首轮）
- 小瑕疵：USER 角色访问管理接口返回 `HTTP 200 + code 500 "Access Denied"`，而非标准 403 语义（权限拦截本身生效，仅响应格式不标准，可调 GlobalExceptionHandler）
- system_config_bak 备份表保留在 swxt 库中，确认无误后可删除

### 2026-08-06 代码现状更新（订单-库存联动改造，未实测）
> 背景：工作区未提交改动，将订单流程从「下单即扣库存」改为「下单 → 操作员按订单出库 / 退货入库」两步。以下为改造内容概述，三轮验证结果见下方「2026-08-06 三轮验证」小节。

- 后端：OrderService.createOrder 不再自动扣减库存（仅校验库存，订单保持 PENDING 待出库）；StockService 新增 stockOutByOrder（扣库存+写 OUT 记录+订单→COMPLETED）与 stockInByOrder（回库存+写 IN 记录+订单→CANCELLED），均 @Transactional；StockController 新增 POST /stock/order/{orderId}/out 与 /in（OPERATOR，写 ORDER 操作日志）
- 订单列表：OrderController.listAll 改为分页 PageResult，支持 status（PENDING/COMPLETED/CANCELLED）与 keyword（用户名/订单号）筛选；OrderMapper 新增 listAllPage/countAll
- 权限：StatisticsController 的 /stock-records 与 /stock-records/search 由仅 ADMIN 开放为 ADMIN+OPERATOR
- 注册：UserService.register 在 allow_register=false 时对 ADMIN/OPERATOR 角色跳过校验（管理员可创建操作员账号）；UserManagement 新建用户角色新增 OPERATOR 选项
- 前端：新增 OrderManagement.vue（操作员订单管理：分页/状态筛选/关键词/详情，路由 /operator/orders，OperatorHome 菜单加「订单管理」）；StockManager.vue 改双标签「商品出入库 / 订单出入库」（订单 tab 可直接出库/退货入库/详情）；Pagination.vue 页码序列改为当前页前后连续页码；style.css 新增 tab-bar、status-tag 等样式

### 2026-08-06 三轮验证（订单-库存联动改造后首次实测，本次）
> 环境：后端 8887 端口运行中、MySQL 8.0.46、JDK 17；`mvn test`（BUILD SUCCESS）与 `npm run build`（vue-tsc + vite，含新 OrderManagement 组件）均通过。

- 订单-库存联动闭环全通过：下单仅校验库存不扣减（商品 1 下单前后均 120，订单 PENDING）→ POST /stock/order/{id}/out 出库（120→119，订单→COMPLETED）→ POST /stock/order/{id}/in 退货入库（订单→CANCELLED，库存回补）
- 订单列表分页筛选正常：GET /orders?page=&size=&status=PENDING&keyword=zhangsan（PageResult 分页结构）
- 权限开放生效：OPERATOR 访问 GET /statistics/stock-records 返回 200（2026-08-06 由仅 ADMIN 开放）
- 注册开关：allow_register=false 时公开注册被拒（code 1001「系统已关闭注册」），管理端 POST /register?usermanager=1 创建 OPERATOR 成功（问题 #7 修复后复验）；验证后已恢复 allow_register=true
- 残留数据已清理：2 条测试订单及明细、3 条订单出入库记录、r3* 测试账号已删，商品 1 库存恢复 120
- 脚本注意（非应用 bug）：PowerShell 单元素数组会被 `ConvertTo-Json` 折叠成对象、body 含中文字段会破坏 UTF-8，实测 PUT /system-config 需手动拼数组且传 ASCII 字段（前端 Vue 实际工作正常）

## 六、代码问题清单（2026-08-05 二轮 + 2026-08-06 三轮验证发现）

1. **CategoryController.java:47（已修复）** — create() 失败分支误记 `Core.LogResult.SUCCESS`，已改为 FAIL
2. **GlobalExceptionHandler.java（已修复）** — 新增三个处理器：① AccessDeniedException → 403（越权访问不再返回 500「Access Denied」）；② NoResourceFoundException → 404（路径不存在时不再返回 500「系统繁忙」）；③ HttpRequestMethodNotSupportedException → 405（路径存在但方法不匹配，如 GET /products/{id} 仅有 DELETE）
3. **Core.TargetType + OrderController.java:39、82（已修复）** — TargetType 枚举新增 ORDER，订单创建/改状态日志不再误用 PRODUCT
4. **OrderController.java:65（已修复）** — GET /orders/{id} 增加 `@PreAuthorize("hasAnyRole('OPERATOR','ADMIN')")`，普通用户越权查他人订单被拒（实测 403）
5. **UserController.java:64-65（已修复）** — register 改为 try/catch RuntimeException 记录 FAIL 后重抛，注册失败（含用户名重复 409）现会写入 REGISTER/FAIL 日志（实测 total=1）
6. **Cart.vue:166-176（已修复）** — checkout() 改为调用 POST /orders 真正下单（扣库存），成功才清空购物车；失败展示后端 message
7. **UserService.java:36-39（已修复并复验）** — 2026-08-06 注册跳过校验改造引入回归：条件多加了 `&& request.getRole() != Core.Role.USER`，导致 `allow_register=false` 时公开注册普通 USER 账号仍被放行（三轮实测 success=true code=200）；已删除该多余条件（关闭注册时仅管理端 usermanager=1 可创建账号），修复后实测公开注册被拒 code=1001、管理端创建 OPERATOR 成功
8. **越权访问 HTTP 状态码（小瑕疵，暂未改）** — USER 越权访问管理接口实测返回 `HTTP 200 + body code=403「无权访问」`，语义正确但非标准 HTTP 403（二轮时 body 为 code=500，已改进）；如需标准 403 可将 GlobalExceptionHandler 的权限处理器改为 `ResponseEntity.status(403)`
